spring:
  application:
    name: journalApp
  data:
    mongodb:
#      uri: mongodb://chirayu:Chirayu@localhost:27018/testdb?authSource=admin
      uri: ${MONGO_URL}
      auto-index-creation: true
  redis:
    host: localhost
    port: 6379
  kafka:
    bootstrap-servers: pkc-921jm.us-east-2.aws.confluent.cloud:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
#      value-serializer: org.apache.kafka.common.serialization.JsonSerializer
    consumer:
      group-id: weekly-sentiment-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
#      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      properties:
        spring:
          json:
            trusted:
              packages: com.ChirayuTech.journalApp.model

    properties:
      security:
        protocol: SASL_SSL
      sasl:
        mechanism: PLAIN
        jaas:
          config: org.apache.kafka.common.security.plain.PlainLoginModule required username='BJL434L3M576ADI4' password='cflthscttElgH9eI4WoJikMYgiG0JKTfD3BNVIwaQqCiob7g3FlARca6ilRYFQOQ';
      session:
        timeout:
          ms: 45000
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SEND_EMAIL} # give from sender email.
    password: ${APP_PASSWORD} # google Security App password can work fine.
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
